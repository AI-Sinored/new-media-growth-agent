[project]
name = "content-growth-agent-mvp"
version = "0.1.0"
description = "A runnable MVP for an AI-driven content growth agent."
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
